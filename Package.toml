# package configurations
[package]
    # package name
    name =                 "tree-punching-simulator"
    # contributors
    author =               "I'mTrevor#6666"
    # version
    version =              "0.0.1"
    # image URL
    image =                "https://i.imgur.com/B9IKTvL.png"
    # package type: 'script' (normal package), 'game-mode' (unique package - can only load one at a time) or 'loading-screen' (special package loaded in loading screen)
    type =                 "script"
    # whether to force the custom map Script to do not load
    force_no_map_script =  true
    # auto destroy all entities spawned by this package when it unloads
    auto_cleanup =         true
    # packages requirements
    packages_requirements = [
    ]
    # asset packs requirements
    assets_requirements = [
        
    ]
